{
    "name": "rekindle-build",
    "version": "1.0.0",
    "scripts": {
        "build": "node build-automation.js",
        "build:badwords": "node scripts/build-bad-words.js",
        "deploy:beta": "npx wrangler pages deploy . --project-name=rekindle --branch=beta",
        "deploy:rules:firestore": "firebase deploy --only firestore:rules",
        "deploy:rules:rtdb": "firebase deploy --only database:rules",
        "deploy:rules:storage": "firebase deploy --only storage",
        "deploy:rules:social:firestore": "firebase deploy --config firebase-social.json --only firestore:rules --project rekindle-socials",
        "deploy:rules:social:rtdb": "firebase deploy --config firebase-social.json --only database:rules --project rekindle-socials",
        "deploy:rules:social": "npm run deploy:rules:social:firestore && npm run deploy:rules:social:rtdb"
    },
    "dependencies": {
        "@babel/core": "^7.23.0",
        "@babel/preset-env": "^7.23.0",
        "cheerio": "^1.0.0-rc.12",
        "firebase-admin": "^13.7.0",
        "fs-extra": "^11.3.3",
        "glob": "^10.3.0",
        "postcss": "^8.5.10",
        "postcss-preset-env": "^10.5.0",
        "stripe": "^20.3.0"
    },
    "overrides": {
        "fast-xml-parser": "^5.7.0",
        "@tootallnate/once": "^3.0.1"
    },
    "devDependencies": {
        "esbuild": "^0.28.0",
        "obscenity": "^0.4.6"
    }
}
